{
  "name": "git-server-repo",
  "version": "1.2.1",
  "description": "A custom git command which creates a remote repository specifically for git deployments.",
  "main": "bin/git-server-repo/git-server-repo.js",
  "bin": {
    "git-server-repo": "bin/git-server-repo/git-server-repo.js"
  },
  "dependencies": {
    "chalk": "^2.3.0",
    "logger": "0.0.1",
    "shelljs": "^0.8.1",
    "yargs": "^10.1.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "spear.spencer@gmail.com",
  "license": "ISC"
}
